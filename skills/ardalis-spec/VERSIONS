# One full version per line: MAJOR.MINOR.PATCH.
# This file is an ACCUMULATING ledger — each detected version stays here
# forever (it is NOT a lockfile that gets overwritten on every patch).
# Generated reference files for every entry remain on disk as historical
# reference, so a project pinned to an older patch still has its docs.
#
# The update script auto-adds NEWER versions only (forward-only):
#   - new patches in already-tracked minors (v9.3.2 added when v9.3.1 tracked)
#   - new minors in already-tracked majors  (v9.4.0 added when v9.3.x tracked)
#   - new majors entirely                   (v10.0.0 added when v9.x tracked)
# OLDER versions you never opted into are never backfilled automatically;
# add them manually here if you want to start tracking historical patches.
#
# To stop generating files for a minor: delete its line(s). The existing
# generated files stay on disk; remove them by hand if you really want them gone.
8.0.0
9.3.1
